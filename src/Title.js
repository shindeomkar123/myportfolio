const Title = ({ title }) => {
  return (
    <div className="h-20  m-10 flex justify-center">
      <span className="text-purple-900 text-5xl">{title}</span>
    </div>
  );
};

export default Title;
